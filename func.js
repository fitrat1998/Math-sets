//mover
let elem1 = document.getElementById('hiddenInput');
let rect = elem1.getBoundingClientRect();
console.log(rect.left);


let right = document.getElementById('birlashma');

right.addEventListener("click",function(){

  let elements = document.querySelectorAll('.rand1');
  //console.log("click")
  elements.forEach((element,index)=>{

    setTimeout(()=>{
      element.style.left = (parseInt(element.style.left) + rect.left -30)+"px";
    }, index* 800)
  })

let elements1 = document.querySelectorAll('.rand2');
  //console.log("click")
  elements1.forEach((element,index)=>{
    setTimeout(()=>{
      element.style.left = (parseInt(element.style.left) + rect.left -30)+"px";
    }, index* 1000)
  })
});
//mover

//mover
let elem1 = document.getElementById('hiddenInput');
let rect = elem1.getBoundingClientRect();
console.log(rect.left);


let right = document.getElementById('ayirmaA');

right.addEventListener("click",function(){

  let elements = document.querySelectorAll('.rand1');
  //console.log("click")
  elements.forEach((element,index)=>{

    setTimeout(()=>{
      element.style.left = (parseInt(element.style.left) + rect.left -30)+"px";
    }, index* 800)
  })

let elements1 = document.querySelectorAll('.rand2');
  //console.log("click")
  elements1.forEach((element,index)=>{
    setTimeout(()=>{
      element.style.left = (parseInt(element.style.left) + rect.left -30)+"px";
    }, index* 1000)
  })
});
//mover

//mover
let elem1 = document.getElementById('hiddenInput');
let rect = elem1.getBoundingClientRect();
console.log(rect.left);


let right = document.getElementById('ayirmaB');

right.addEventListener("click",function(){

  let elements = document.querySelectorAll('.rand1');
  //console.log("click")
  elements.forEach((element,index)=>{

    setTimeout(()=>{
      element.style.left = (parseInt(element.style.left) + rect.left -30)+"px";
    }, index* 800)
  })

let elements1 = document.querySelectorAll('.rand2');
  //console.log("click")
  elements1.forEach((element,index)=>{
    setTimeout(()=>{
      element.style.left = (parseInt(element.style.left) + rect.left -30)+"px";
    }, index* 1000)
  })
});
//mover

//mover
let elem1 = document.getElementById('hiddenInput');
let rect = elem1.getBoundingClientRect();
console.log(rect.left);


let right = document.getElementById('kesishma');

right.addEventListener("click",function(){

  let elements = document.querySelectorAll('.rand1');
  //console.log("click")
  elements.forEach((element,index)=>{

    setTimeout(()=>{
      element.style.left = (parseInt(element.style.left) + rect.left -30)+"px";
    }, index* 800)
  })

let elements1 = document.querySelectorAll('.rand2');
  //console.log("click")
  elements1.forEach((element,index)=>{
    setTimeout(()=>{
      element.style.left = (parseInt(element.style.left) + rect.left -30)+"px";
    }, index* 1000)
  })
});
//mover

